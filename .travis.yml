language: node_js
node_js:
  - '10'
services:
  - postgresql
env:
  - NODE_ENV=test
before_install:
  - psql -c 'create database circles_test;' -U postgres
install:
  - npm install
  - npm run db:migrate
  - npm run db:seed
script:
  - npm test
